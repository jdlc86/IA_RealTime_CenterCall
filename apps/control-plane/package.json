{
  "name": "@ia-realtime-centercall/control-plane",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "types": "wrangler types",
    "check": "wrangler deploy --dry-run",
    "test": "rm -rf .test-dist && tsc src/tenant-resolver.ts --target ES2022 --module ES2022 --moduleResolution bundler --outDir .test-dist --skipLibCheck && node --test src/*.test.mjs"
  },
  "dependencies": {
    "openai": "^7.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.0.0"
  }
}
